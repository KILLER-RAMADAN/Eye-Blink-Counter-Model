install all libraries to run python script

by following command in your terminal=>(pip install <name of the laibrary>)


################################################
import cv2 (laibrary)
import cvzone (laibrary)
################################################